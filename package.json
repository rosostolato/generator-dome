{
  "name": "generator-dome",
  "version": "1.2.1",
  "description": "",
  "scripts": {
    "build": "copyfiles package.json README.md generators/**/* dist",
    "publish": "cd ./dist && npm publish",
    "release": "npm run build && npm run publish"
  },
  "files": [
    "generators"
  ],
  "keywords": [
    "yeoman-generator",
    "dome"
  ],
  "dependencies": {
    "plur": "^4.0.0",
    "yeoman-generator": "^4.8.3"
  },
  "devDependencies": {
    "copyfiles": "^2.2.0"
  }
}
